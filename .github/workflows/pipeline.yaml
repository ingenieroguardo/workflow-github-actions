name: Pipeline
run-name: deploy in live by ${{github.actor}}
on:

  push:
    branches:
      - main
   
  pull_request:
    branches:
      - main
    paths:
      - "*.md"

jobs:
  jobs1:
    environment: ${{ github.ref_name == 'main' && 'production' || 'developer' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.1

      - name: Print message
        run: echo "Hello, Algo-${{vars.NAME}}-Máss!"

      - name: echo secreto
        run: echo ${{secrets.SECRETO}} > secret.txt

      - name: Echo file
        run: cat secret.txt

  

   
  jobs2: 
    environment: 
      name: production
    needs: jobs1
    runs-on: ubuntu-latest
    steps:
      
      - name: Checkout code
        uses: actions/checkout@v6.0.1

      - name: Print message
        run: echo "Hello, Algo-${{vars.NAME}}-Máss!"

      - name: echo secreto
        run: echo ${{secrets.SECRETO}} > secret.txt

      - name: Echo file
        run: cat secret.txt
  
  jobs3: 
      
    needs: jobs2
    runs-on: ubuntu-latest
    steps:
      
      - name: Checkout code
        uses: actions/checkout@v6.0.1

      - name: Print message
        run: echo "Hello, Algo-${{vars.NAME}}-Máss!"

     

